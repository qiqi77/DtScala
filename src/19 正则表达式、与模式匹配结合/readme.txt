正则表达式 

正则表达式 和 模式匹配结合使用。
正则表达式 模式匹配  简化正则表达式的代码

在数据清洗中很重要 (ETL)  正则表达式语法

[0-9]+  :可以是0-9中的数字，可以有若干个。  +（加号）表示 可有若干个。

val regex = """([0-9]+) ([a-z]+)""".r        //.r表示 将其变为正则表达式。可在worksheet中测试

程序中很多操作 都可在worksheet 实验 观察结果。
